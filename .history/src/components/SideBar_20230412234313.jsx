import { Stack } from "@mui/material"
import { categories } from "../utils/constants"
const SideBar = () => {
  return (
    <Stack direction='row' sx={{overflow: 'auto', height: {sx: 'auto', md: '92vh'}}}>

    </Stack>
  )
}

export default SideBar